# Windows Streamlit app — no models loaded locally
streamlit>=1.35.0
requests
Pillow>=10.0.0
pymupdf          # PDF preview + conversion
python-docx      # .docx reading
openpyxl         # .xlsx reading
numpy